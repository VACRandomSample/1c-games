 Перем КтоЗаходит;
Процедура ПередНачаломРаботыСистемы(Отказ)
	КтоЗаходит="";
	ВвестиЗначение(КтоЗаходит,"Представьтесь");
	
	Если ЗначениеЗаполнено(КтоЗаходит) Тогда
		Если БиблиотекаАлгоритмов.ОпределитьИгрока(КтоЗаходит)=Ложь Тогда
			Отказ=Истина;
		КонецЕсли;
	Иначе
		Отказ=Истина;   
			
	КонецЕсли;
КонецПроцедуры

Процедура ПриНачалеРаботыСистемы()
	УстановитьКраткийЗаголовокПриложения(КтоЗаходит);
КонецПроцедуры
